# App

This directory contains the main application files.

## `layout.tsx`

The main layout for the application.

## `page.tsx`

The main page for the application.

## `globals.css`

The global CSS file for the application.

## `favicon.ico`

The favicon for the application.
