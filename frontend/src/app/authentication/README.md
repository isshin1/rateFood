# Authentication

This directory contains the sign-in and sign-up pages for the application.

## Sign In

The sign-in page is located at `/authentication/signin`. It contains a form that allows users to sign in with their email and password.

## Sign Up

The sign-up page is located at `/authentication/signup`. It contains a form that allows users to create a new account with their username, email, and password.
