# Figma Components

This directory contains components that are based on the Figma design.

## ImageWithFallback

An image component that displays a fallback image if the original image fails to load.
